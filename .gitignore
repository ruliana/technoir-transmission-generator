node_modules/
dist/
.env.local
.env

# Archive transmission files — add entries to manifest.json instead of committing data files
public/archives/*.json
public/archives/*.json.gz
# But DO track the manifest and README
!public/archives/manifest.json
!public/archives/README.md
